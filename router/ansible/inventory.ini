# Basic inventory for the router automation project.
# The playbook loads router/config/router-config.local.yml via -e @file, so the
# ansible_host and ansible_user are overridden by that file.

[router]
router

[router:vars]
ansible_python_interpreter=/usr/bin/python3
